name: Deploy to Azure VM

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allows manual trigger

jobs:
  test:
    name: Test Backend
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install backend dependencies
      run: |
        cd backend
        pip install -r requirements.txt
    
    - name: Run backend tests
      run: |
        cd backend
        pytest test_server.py -v
    
    - name: Run backend linting
      run: |
        cd backend
        flake8 server.py --max-line-length=120 --ignore=E402,W503 || echo "⚠️ Linting warnings found"

  deploy:
    name: Deploy to Azure VM
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup SSH
      env:
        SSH_KEY: ${{ secrets.AZURE_VM_SSH_KEY }}
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
      run: |
        echo "🔑 Setting up SSH connection..."
        echo "VM Host: $VM_HOST"
        echo "VM User: $VM_USER"
        
        mkdir -p ~/.ssh
        echo "$SSH_KEY" > ~/.ssh/azure_key
        chmod 600 ~/.ssh/azure_key
        echo "✅ SSH key configured"
        
        echo "🔍 Adding VM to known hosts..."
        ssh-keyscan -H $VM_HOST >> ~/.ssh/known_hosts 2>&1
        echo "✅ Known hosts updated"
        
        echo "🧪 Testing SSH connection..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST "echo '✅ SSH connection successful'"
    
    - name: Pull Latest Code
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
      run: |
        echo "📥 Pulling latest code from repository..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST << 'ENDSSH'
          set -e
          cd /home/azureuser/truth-quest
          echo "Current directory: $(pwd)"
          git pull origin main
          echo "✅ Code updated successfully"
        ENDSSH
    
    - name: Create Environment File
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
        OPENAI_KEY: ${{ secrets.OPENAI_API_KEY }}
        BRAVE_KEY: ${{ secrets.BRAVE_API_KEY }}
        YOUTUBE_KEY: ${{ secrets.YOUTUBE_API_KEY }}
      run: |
        echo "🔧 Creating .env file with secrets..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST << ENDSSH
          set -e
          cd /home/azureuser/truth-quest/backend
          echo "OPENAI_API_KEY=$OPENAI_KEY" > .env
          echo "BRAVE_API_KEY=$BRAVE_KEY" >> .env
          echo "YOUTUBE_API_KEY=$YOUTUBE_KEY" >> .env
          echo "PORT=3001" >> .env
          echo "GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json" >> .env
          echo "✅ Environment file created"
        ENDSSH
    
    - name: Install Backend Dependencies
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
      run: |
        echo "📦 Installing backend dependencies..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST << 'ENDSSH'
          set -e
          cd /home/azureuser/truth-quest/backend
          source venv/bin/activate
          pip install -r requirements.txt --quiet
          echo "✅ Backend dependencies installed"
        ENDSSH
    
    - name: Build Frontend
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
      run: |
        echo "🏗️ Building frontend..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST << 'ENDSSH'
          set -e
          cd /home/azureuser/truth-quest/frontend
          npm install --silent
          npm run build
          echo "✅ Frontend built successfully"
        ENDSSH
    
    - name: Restart Services
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
        VM_USER: ${{ secrets.AZURE_VM_USERNAME }}
      run: |
        echo "🔄 Restarting services..."
        ssh -i ~/.ssh/azure_key -o StrictHostKeyChecking=no $VM_USER@$VM_HOST << 'ENDSSH'
          set -e
          sudo systemctl restart truth-quest-backend
          echo "✅ Backend service restarted"
          sudo systemctl restart nginx
          echo "✅ Nginx restarted"
          
          # Wait a moment for services to start
          sleep 3
          
          # Check service status
          echo "📊 Service status:"
          sudo systemctl is-active truth-quest-backend || echo "⚠️ Backend service not active"
          sudo systemctl is-active nginx || echo "⚠️ Nginx not active"
        ENDSSH
          
    - name: Verify Deployment
      env:
        VM_HOST: ${{ secrets.AZURE_VM_HOST }}
      run: |
        echo "⏳ Waiting for services to stabilize..."
        sleep 5
        
        echo ""
        echo "✅ Deployment Complete!"
        echo "================================"
        echo "Frontend: http://$VM_HOST"
        echo "Backend API: http://$VM_HOST:3001"
        echo "Health Check: http://$VM_HOST:3001/api/health"
        echo "================================"
